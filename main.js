import { Tasks } from "./js/tasks.js";

// const ul = document.querySelector('.test');

// ul.innerHTML = Tasks.map(
//   (task, index) =>
// 		`<li data-index = '${index}'> <input type="checkbox">
//   ${task.title} ${task.status} ${task.priority}</li>`
// ).join('');